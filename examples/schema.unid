type State =
  Pending
  | Running
  | Complete

type Task = {
  id int,
  title text,
  state State,
  priority int = 0,
}

table tasks Task
  key id

create index tasks (state)

create index tasks (priority)
