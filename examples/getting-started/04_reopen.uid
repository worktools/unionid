from tasks
filter (
  match state {
    Running {worker, attempt} => attempt >= 1,
    _ => false,
  }
)
select {
  id,
  title,
  owner.email,
  state,
  worker = match state {
    Running {worker, ..} => worker,
    _ => "",
  },
}
sort id
